## TODO LIST
1. When I enable/disable the hints with for example <leader>ut it toggles it correctly, however, if I open a new buffer with disabled hints, in new buffer it appears enabled, and if I toggle it again with <leader>ut to disable them again, I get this notification: Enabled **Inlay Hints**
2. Install sql lsp and adjust so prettier (in best scenario, if not possible, should be other). Should format correctly the sql



