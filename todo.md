## TODO LIST

1. Also, is it possible to do so when i press <C-s>r or others, it should automatically focus on the "Search" field if possible
2. When I enable/disable the hints with for example <leader>ut it toggles it correctly, however, if I open a new buffer with disabled hints, in new buffer it appears enabled, and if I toggle it again with <leader>ut to disable them again, I get this notification: Enabled **Inlay Hints**



